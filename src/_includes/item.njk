---
layout: base
title: News Item
menu: news
---

<div
    class="bg-white rounded-sm overflow-hidden shadow flex flex-row h-36 w-36 mx-auto my-8"
    >
    <img
        class="w-36 h-36 object-cover"
        src="{{ image }}"
        alt="{{ title }}"
    />
</div>

<div class="mt-8 mb-4 text-center">
<h3 class="uppercase text-gray-400 font-bold text-2xl">{{ title }}</h3>
</div>
<div class="flex px-8 sm:px-0">
<a href="{{ '/news' | url }}"
class="px-2 py-1 text-xs uppercase font-bold text-gray-500 m-2 hover:bg-gray-400 hover:text-white rounded-sm ml-0"
>
🔙 back
</a>
</div>

<div class="bg-white rounded-sm overflow-hidden shadow">
<div class="markdown-canvas">
    {{ content | safe }}
</div>
</div>

<p class="m-2 ml-0 font-bold text-xs text-gray-400">{{ date.toDateString() }}</p>